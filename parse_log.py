import os
import math
import datetime



source = os.listdir("./planing/")


def planing(fichier_de_recherche):
    new_list = []
    page = "./planing/"+fichier
    page =  open(fichier_de_recherche, "r", encoding="utf-8")
        lignes = page.readlines()
        for line in 
        new = line.strip().split()
